[package]
name = "copyedit-check"
version = "0.1.0"
edition = "2024"
description = "A CLI tool to detect copy-paste and edit errors across programming languages"
license = "MIT"

[dependencies]
clap = { version = "4.0", features = ["derive"] }
ignore = "0.4"
rayon = "1.11.0"
regex = "1.10"
